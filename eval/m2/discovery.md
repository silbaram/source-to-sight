# M2 source readings — 2026-09-06

The six candidates were authored by the coding agent after reading the pinned local checkouts in `.cache/m1-sources`. Repository IDs, commits and licenses use [the existing source manifest](../m1/sources.json). These are recorded source readings, not independent model runs, observed executions, or human-approved answers. Search/read counts beyond the recorded files were not measured.

| Case | Source ranges read | Decisions that affect the explanation |
| --- | --- | --- |
| utility-retry-budget | smolagents `utils.py:528–606`, `models.py:38–41,1161–1202` | The bare helper defaults to one total attempt. ApiModel chooses three total attempts or one with retry disabled; the predicate checks strings. Backoff multiplies before sleeping. Both reraise branches raise. Returned generators are not consumed by this helper. |
| plugin-wrapper-unwind | pluggy `_hooks.py:527–542`, `_manager.py:95–108`, `_callers.py:82–174` | Read the default executor route, reverse implementation traversal, new wrapper entry, non-None firstresult and reverse teardown. Wrappers can replace results or clear exceptions. Concrete plugins and old-style wrappers remain outside scope. |
| library-resize-callbacks | golang-lru `lru.go:33–55,185–201`, `simplelru/lru.go:156–182` | A nonnegative resize removes the positive length difference. Constructor wiring buffers internal eviction notices; public Resize detaches buffers and unlocks before user callbacks. A callback panic has no rollback or continue-all mechanism here. Negative sizes are deliberately outside this count contract. |
| agent-parallel-tools | smolagents `agents.py:296–331,468–637,1276–1518`, `tools.py:231–256` | Per-run positive max_steps overrides the default. Multiple calls use executor submission and completion iteration, then ID-sorted observations. Failure can skip the final group record. General AgentError continuation differs from fatal AgentGenerationError. Follow the ordinary inherited Tool call boundary into another module without inventing its subclass behavior. |
| event-async-lifecycle | blinker `base.py:91–139,168–189,255–403` | Temporary strong registration has finally-disconnect, not prior-state restoration. `send_async` awaits each receiver within its loop; unspecified receiver order is not parallelism. Missing sync wrapper or receiver failure interrupts subsequent delivery. Registration and the caller's later send are separate requests. |
| web-routing-fallbacks | httprouter `router.go:409–542`, `tree.go:326–431` | Matching handler wins. Conditional slash correction precedes fallback handling and uses 301 for GET, 308 otherwise. Auto-OPTIONS and 405 are separate branches; unmatched final path requests 404. Fixed-path correction, custom handlers and server-wide OPTIONS `*` are excluded. |

`author_cases.py` records candidate claims and exact evidence hashes/anchors. `reference_cases.py` independently lists source roles, required claims and path constraints; it only copies question/scope identity to bind evaluation to the chosen task. Neither script discovers arbitrary repositories. Existing reference files are preserved, so a reference change must be made deliberately in both the ledger and the JSON. Reauthoring candidates changes their timestamp/hash and invalidates old reviews.

The mixed-profile checks cover utility/SDK policy composition, Go public/internal packages, Python agent/tool modules, and signal/plugin lifecycles. Existing JavaScript cases remain in the 24-case regression suite. This does not establish universal cross-language process tracing: external implementations and runtime choices remain boundaries, and the guide requires reading both sides before resolving such a relationship.

The agent's normal-group walkthrough explicitly names its existing entry, per-worker call, observation write and loop edges. `agents.py:1336` enters `process_tool_calls`; the helper calls `execute_tool_call` at line 1390; that method invokes the ordinary tool at lines 1485–1488. Normal exhaustion of the group's generator writes observations at lines 1436–1442, and the loop condition at line 545 controls the next iteration. These are relationships in the selected code path, not a measured schedule or an invented order between tools. The fourth step includes both caller and group evidence so the entry itself can be reviewed.

The snapshots contain only public source. Internal candidate JSON retains anchors for revalidation; generated HTML contains locations and descriptions, not anchors or source bodies. All six new results stay `partial` because their selected scopes preserve dynamic implementation or external-result limits. Human facts and comprehension review remains pending.
