# M3 source rereadings — 2026-09-07

The coding agent reread the pinned local sources in `.cache/m1-sources` before extending the six M2 behavior cases with rules, reasons and exceptions. Source identities and licenses remain in [the existing source manifest](../m1/sources.json). The original 24 behavior candidates and references are preserved. These are recorded source readings and calculated examples, not observed program executions, model-generation experiments or human-approved answers.

| Rules case | Source reread for M3 | Contract checked |
| --- | --- | --- |
| utility-retry-budget-rules | smolagents `utils.py:528–606`, `models.py:38–41,1161–1202` | The helper's default is one total attempt; ApiModel chooses three with retries enabled and one when disabled. Remaining budget and the retry predicate control another attempt. Backoff updates before the wait, and jitter is not a fixed interval. |
| plugin-wrapper-unwind-rules | pluggy `_callers.py:82–174` | `firstresult` stops the implementation loop only for a non-None result. Entered wrappers still unwind in reverse order. A wrapper's returned value can replace the result and clear an exception; concrete wrapper behavior remains outside scope. |
| library-resize-callbacks-rules | golang-lru `lru.go:185–201`, `simplelru/lru.go:156–182` | For nonnegative sizes, removal follows the positive length difference. The 4→2 and 2→4 input examples are calculations, not execution observations. Capacity and removals are recorded before unlocking and user callbacks. Callback panic has no rollback/continue-all contract here. |
| agent-parallel-tools-rules | smolagents `agents.py:296–331,468–499,540–612,1361–1442` | A supplied positive per-run limit overrides the configured limit. Omitting it uses the constructor's setting; 20 is the constructor default, not a forced value. One loop step differs from the number of tools. General AgentError handling differs from fatal generation errors. Completion collection and ID-sorted recording are separate. |
| event-async-lifecycle-rules | blinker `base.py:168–189,255–303,326–403` | Coroutine receivers are awaited directly; sync receivers need an adapter. Receiver failure interrupts later delivery. Temporary registration is strong and finally-disconnected, not restored to its previous registration state. |
| web-routing-fallbacks-rules | httprouter `router.go:463–542` | Checked method/settings select auto-OPTIONS or 405. An empty auto-OPTIONS result does not enter the sibling 405 branch. Redirect returns before fallback handling. Without a custom fallback, the final unmatched request selects 404. |

Existing M2 evidence also remains intact, including constructor/callback wiring, public entrypoints and module boundaries previously read. New rules cite the current source ranges; reasons explain the implementation mechanism and do not invent the developer's motivation. Unread custom handlers, tool implementations, model results and wrapper/receiver bodies stay explicit limits. Permissions are a discovery question when applicable, not an invented rule in these cases.

`author_cases.py` records six logic graphs and their presentation layouts. Each begins with `author.py explain` from the internal M2 graph, rereads source locations and enriches the reviewed rules. It retains the same subject, scope, language, nodes, edges, scenarios, states and prior evidence. There are 27 illustrated rules: 5 utility, 3 plugin, 5 library, 5 agent, 5 event and 4 web. Of these, 18 retain existing rule meaning and 9 add source-backed contracts or explicitly calculated examples.

The layout selects comparison, condition/result and state figures in topic-specific order. It holds references and headings, not a second factual graph. State diagrams use existing transitions. Conditions remain visible in full, including when a comparison option is selected. No execution edges between rules or fixed playback sequence are introduced.

Reference criteria are explicitly authored by source role, representative line, status, numeric values and required reasons/exceptions, rather than derived from candidate rule IDs or prose. Existing behavior requirements are retained to bind the explanation to the same graph. Required facts and forbidden claims require a separate human judgment. The same coding agent authored candidate and reference, so they are not independent acceptance evidence. Reference files are preserved on rerun; deliberate corrections must update both the ledger and JSON after source review.

`scripts/evaluate_rules.py` replays all 24 behaviors plus these six paired outputs in a new directory. Rule coverage uses one-to-one matching, and review hashes include the rules, layout, base behavior and both skills. Numerical content removed by verification also removes dependent figures. Source bodies and anchors remain only in internal evidence, never in shared HTML or render JSON. All six rules pages remain `partial`; independent fact and comprehension reviews are pending. Search/read counts, model identity, generation time, tokens and cost were not measured.
